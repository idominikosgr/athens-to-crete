Athens to Crete reminder: don't build roads across seas, don't invent shipcopters - use boats. Before implementing: (1) understand the actual problem, not the stated request; (2) map real constraints; (3) reuse existing solutions in the codebase; (4) verify the path end-to-end. Code that compiles ≠ code that solves the problem. No orphaned code, no silent failures, no v2 file variants - refactor in place.
